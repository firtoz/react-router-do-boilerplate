{
	"name": "hono-do-fetcher",
	"version": "0.0.1",
	"private": true,
	"type": "module",
	"main": "./src/index.ts",
	"scripts": {
		"test": "npx vitest --run",
		"test:watch": "npx vitest"
	},
	"dependencies": {
		"hono": "^4.6.5"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20241018.0",
		"@types/bun": "^1.1.11",
		"ts-miniflare": "^0.2.0",
		"vitest": "^2.1.3"
	}
}
